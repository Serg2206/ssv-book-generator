
openai>=1.0.0  # Для генерации текста через OpenAI
# anthropic>=0.5.0 # Для генерации текста через Anthropic (опционально)
pillow # Для обработки изображений (обложки, иллюстрации)
pyyaml # Для работы с book_config.yaml
python-dotenv # Для загрузки .env
requests # Для HTTP-запросов (API генерации изображений)
# moviepy>=1.0.3 # (опционально, если будет генерироваться аудио/видео контент)
# ebooklib # Для работы с EPUB
# fpdf2 # Для генерации PDF
# weasyprint # Для рендеринга HTML в PDF (с CSS)
# latex # Для сложных научных формул (требует отдельной установки)
